{
  "scripts": {
    "start": "node api.js",
    "json-server": "npx json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "express-rate-limit": "^8.5.1",
    "helmet": "^8.1.0"
  }
}
